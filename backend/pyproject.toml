[project]
name = "cad-backend"
version = "0.1.0"
description = "Backend service for the CAD project"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
]

[tool.pytest.ini_options]
testpaths = ["tests"]

